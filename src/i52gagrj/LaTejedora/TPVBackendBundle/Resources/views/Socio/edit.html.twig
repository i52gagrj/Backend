{% extends '::base.html.twig' %}

{% block contenido %}
<div class="contenido">  
    <h1>Editar Socio </h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('socio') }}">
            Volver al listado
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
</div>
{% endblock %}

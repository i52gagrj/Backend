{% extends '::base.html.twig' %}

{% block contenido %}
<div class="contenido">
    <h3>Lista de Productos</h3>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Descripcion</th>
                <th>Precio</th>
                <th>IVA</th>
                <th>Stock</th>
                <th>Activo</th>
                <th>Fecha de baja</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('producto_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombre }}</td>
                <td>{{ entity.descripcion }}</td>
                <td>{{ entity.precio }}</td>
                <td>{{ entity.iva }}</td>
                <td>{{ entity.stock }}</td>
                <td>{%if entity.activo %}Si{% else %}No{% endif %}</td>
                 <td>{% if entity.activo == false %}{% if entity.fechainactivo %}{{ entity.fechainactivo|date('Y-m-d') }}{% endif %}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('producto_show', { 'id': entity.id }) }}">Mostrar</a>
                    </li>
                    <li>
                        <a href="{{ path('producto_edit', { 'id': entity.id }) }}">Editar</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('producto_new') }}">
                Añadir un nuevo producto
            </a>
        </li>
    </ul>
    </div>
    {% endblock %}

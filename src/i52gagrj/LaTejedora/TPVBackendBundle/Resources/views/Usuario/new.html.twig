{% extends '::base.html.twig' %}

{% block contenido %}
<div class="contenido">
    <h3>Añadir Usuario</h3>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('usuario') }}">
            Volver al listado
        </a>
    </li>
</ul>
</div>
{% endblock %}

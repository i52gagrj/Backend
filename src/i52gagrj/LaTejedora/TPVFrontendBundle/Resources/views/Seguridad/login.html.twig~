{% extends '::base.html.twig' %}
{% block contenido %}
  <div class="contenido"> 
{% if error %}
    <div>{{ error.messageKey|trans(error.messageData) }}</div>
{% endif %}

<form action="{{ path('login_check') }}" method="post">
  <div>
    <label for="username">Usuario:</label>
    <input size="8" type="text" id="username" name="_username" value="{{ last_username }}" align="right"/>
  </div>
  <div>
    <label for="password">Clave: &nbsp &nbsp</label>
    <input size="8" type="password" id="password" name="_password" align="right"/>
  </div>

    {#
        If you want to control the URL the user
        is redirected to on success (more details below)
        <input type="hidden" name="_target_path" value="/account" />
    #}
    <input type="submit" class="symfony-button-grey" value="Login" />
    {# <button type="submit">login</button> #}
  </form>
  </div> 
{% endblock %}
